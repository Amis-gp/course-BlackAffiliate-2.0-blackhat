/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone',
  poweredByHeader: false,
  reactStrictMode: true,
  experimental: {
    // optimizeCss: true
  }
}

module.exports = nextConfig